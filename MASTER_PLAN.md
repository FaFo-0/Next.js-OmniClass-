# OmniClass — Platform Reference

> **What:** OmniClass — multi-tenant language-academy SaaS. First tenant: **Omnica English** (Russian/Arabic-L1 students learning English). Owner: Mustafa (FaFo).
> **Live:** https://next-js-omni-class.vercel.app · Convex prod `valuable-loris-929` · Org `org_3DIbJAWeR5CjVaBRlB4AZXL1UpD`
> **Stage:** pre-launch. Zero real students; target ~50. Don't build for scale that doesn't exist.
> **Business rules:** [POLICY.md](POLICY.md) is the single source of truth for payments, cancellation, no-show, pauses, trials. This file never restates policy — it links to it.
> **AI behavior rules:** [CLAUDE.md](CLAUDE.md).

---

## 1. How work happens here (FaFo's workflow — respect it)

1. **No phases, no steps, no roadmaps, no audits.** FaFo opens a page, finds things he wants changed, dumps them in chat. We change them. That's the whole process.
2. **Always ship at the end of a task**, without asking: `npx convex deploy` (backend first — it does NOT auto-deploy), then commit + `git push origin master` (Vercel auto-builds the frontend). Flag destructive schema changes in the summary but ship them.
3. **Build now, test at the very end.** Phone/mobile pass is dead last. **i18n: English, Russian and Arabic (RTL) are live in the student portal**; staff portals are English until their copy settles.
4. Don't refactor unrelated code. No over-engineering, no premature abstractions.
5. Update §5 Known Issues and the Change Log (§7) when work lands. Tag entries `[Claude]` / `[AI-Name]`.

**Standing technical rules (each one earned by a real bug):**

| Rule | Why |
|---|---|
| **`useQuery` comes from `convex-helpers/react/cache/hooks`, not `convex/react`.** Plain `useQuery` unsubscribes on unmount, so every tab switch refetched from scratch and the UI sat empty for a beat. | Fixed 2026-07-29. |
| **Never push server content back into a live TipTap editor.** The parent saves on a debounce and the server echoes a new object; applying it overwrites whatever was typed since. Ignore the echo, ignore updates while focused. | "Text deletes itself while writing" — fixed 2026-07-29. |
| **Never `new Date(date + "T" + time)` without a timezone.** Stored schedule times are **academy wall-clock** (anchor `Asia/Almaty`, no DST), not UTC. Convert via `wallTimeToMs` / `zonedToInstant` (`convex/lib/time.ts`, `src/lib/tz.ts`). | Same bug fixed 7+ times (crons, ICS, booking notice, slot filters, student dashboard). |
| **Reading model is locked:** a word is on the student's list (green) or it is prose. Never reintroduce per-word statuses (known/ignored/new). One `ReadingView` component for all portals — never fork it. | LingQ-style statuses built then removed 2026-07-29 as UX noise. |
| **The word list IS the flashcards.** `srsCards` is the single source; My Words, study queue, reading tint all read it. A card's answer is the translation into the student's L1 (`users.getLearnerLocale` resolves it: onboarding L1 → UI locale → null). | Two parallel systems drifted; unified 2026-07-29. |
| **"Points" are dead as a user-facing word.** Students see **lessons** ("8 lessons left"). Internals still use point tables at 1 lesson = 1 unit. | FaFo decision 2026-07-14. |
| Tailwind `inset-inline-*`/`inset-x-*` utilities generate no CSS in this setup — use inline `insetInlineStart/End` for absolute overlays. | Calendar bands collapsed to 2px. |
| Use logical CSS properties (`ms-`, `me-`, `ps-`, `pe-`) for future RTL. English first, then Russian, then Arabic. | Arabic market. |
| Never hardcode tenant branding — colors/name live in `tenantSettings`. | Multi-tenant. |
| All Convex access via `convex/lib/tenant.ts` (`requireTenant` / `requireTenantPermission` / `tenantTable`) — no raw cross-org `ctx.db` in feature code. | Tenancy isolation. |
| If style edits don't show in dev: delete `.next` (Turbopack stale cache). | Hit 2026-07-18. |

---

## 2. Architecture

Thin Next.js 16 shell (App Router, Turbopack) over **Convex**. Three portals — Student / Teacher / Admin — gated by **Clerk Organizations** (`org:admin|teacher|student`; JWT template `convex` carries `org_id`). Every row is org-scoped by `organizationId`. Styling: Tailwind v4 + shadcn/ui; brand = yellow `#FFCA00` canvas, purple `#6716A4` primary, dark-purple sidebar; font Inter. STT: Soniox v4 (`@soniox/speech-to-text-web`). LLM: OpenRouter (`OPENROUTER_API_KEY`), prompts per-org in `promptConfigs` with code fallbacks. i18n: next-intl, locales **en + ru + ar** (`src/i18n/config.ts`); Arabic sets `dir="rtl"` on `<html>`. Catalogue in `messages/` — all three files carry every key the student portal uses. Nav labels resolve per-item key against the `nav` namespace with the English config label as fallback.

**Key directories**

```
src/app/(student|teacher|admin|onboarding)   pages per portal
src/components/  shared/ (sidebar, nav, pills) · library/ (ReadingView, WordLookupPopover)
                 calendar/ (WeeklyCalendar, MonthCalendar, calendarShared) · homework/ (HomeworkEditor)
                 recording/ (RecordingPanel) · ui/ (shadcn)
src/lib/         auth · tz · timeFormat · transcript · soniox/ · ai/ · brand/ · format/
convex/          one file per domain (see §4) · lib/(tenant|policy|time|sm2|permissions|defaultPrompts)
scripts/dev-login.mjs   messages/   POLICY.md
```

---

## 3. Platform inventory — what exists today

### Student portal
| Page | What it does |
|---|---|
| `/student` | Dashboard: next-lesson card (real instants, viewer tz, Meet button only when link exists; date shown when >2h away), study-due card, stat cards (completed lessons, words, cards reviewed, lessons left), streak in header, recent published lessons. |
| `/student/lessons` (+`[id]`) | Lesson **history** from schedule events — Done / Upcoming / You missed it / Cancelled, in the student's timezone, filters + search; rows with published notes open them (AI summary, vocabulary, flashcards, self-graded quiz). |
| `/student/library` (+`[id]`) | Material cards w/ CEFR filter → shared ReadingView. Tap word → popover (dictionary + IPA + audio + L1 translation + ✨ contextual AI gloss) → "Add to my words". Green tint = on list. |
| `/student/study` | Hub: open homework, flashcards due (Start), reading recommendations. Session: SM-2 flip cards, translation-first backs, Again re-drills in-session (first rating = the recorded one), Space/1-4 keyboard, 60/session + 15 new/day caps; done screen shows unique cards + real streak. |
| `/student/vocabulary` "My Words" | The one word list: search, not-studied/learning/learned filters (learned = SM-2 interval ≥ 21d), inline translation edit, remove, due badge, "Study N due". |
| `/student/homework` (+`[id]`) | Full homework history (to-do / waiting review / completed w/ score) with due-date pills + standalone editor page: fill-blanks, multi-choice, open answers → submit → teacher feedback. |
| `/student/calendar` | Own lessons + assigned teacher's open ranges (viewer tz). Book (≥12h notice, ≤28d, 15-min grid, repeat-weekly option), policy-aware cancel/move with consequence labels, balance chip + horizon. |
| `/student/achievements` | Gallery with real progress bars, closest-to-done first; unlocks on open + after any qualifying event, with a notification. |
| `/student/billing` | Lesson packs with real catalogue prices (local currency where set) + balance and next expiry. **How to pay** card shows the academy's Kaspi number, recipient name and QR (POLICY §3 v1) so nobody has to ask. **Request** notifies every admin. |
| `/student/profile` | Reached from the avatar menu (Clerk popup carries a Profile link next to Manage account / Sign out). Shared `AccountCard` (name / timezone / 12-24h / phone; native language is shown but staff-set), lesson balance linking to packs, ICS subscribe URL. |
| `/student/book` | Legacy → redirects to calendar. |
| `/onboarding/student` | 3-step wizard: contact + **timezone** (auto-detected, editable) · CEFR (described, not bare codes) + **native language** + goal + interests · availability (days/times chips) + referral + **POLICY §8 recording consent** → trial grant. |
| `/onboarding/teacher` | 3-step wizard: timezone + phone · **meeting room** (Meet link) · weekly working hours → writes real `teacherVacancies`. Gated like the student flow. |

### Teacher portal
| Page | What it does |
|---|---|
| `/teacher` | Dashboard: setup checklist (self-completing), today's classes, recent recordings, this-month earnings (30% share, POLICY §4), stat cards. |
| `/teacher/sessions` | Upcoming (start window: 2h before → 30min after end; Resume if already live) + Past. Start session creates real dated event if unscheduled (`createOneTimeLesson`). Inline rename. |
| `/teacher/sessions/[id]` | Review: transcript+notes, AI summary, editable vocabulary table (→ auto-flashcards on publish), homework editor w/ AI generation. Publish requires summary+vocab approved. |
| `/teacher/sessions/[id]/live` | Full-screen live room: Soniox STT (mic / mic+Meet tab / upload), stable Teacher/Student speaker labels, pause, timer; tabs: Reading / Quiz-from-transcript / AI conversation questions / auto-saved notes; share windows (`/teacher/share/quiz|reading`) for Meet screen-share; no-show flow; discard (= un-start, POLICY-correct); End → review. |
| `/teacher/students` (+`[id]`) | Roster w/ level, lessons left, next lesson, last seen, homework flags. Detail: balance/expiry, lesson stats, profile (incl. one-click **native language** setter — sweeps untranslated cards), homework counts, recent lessons, live local clock + flag. |
| `/teacher/library` (+`[id]?studentId=`) | Same grid as student. "Reading with:" student picker → words go to that student's list, translated into their L1; warning pill links to profile when L1 unset. |
| `/teacher/calendar` | Availability painting (brush + drag + undo, weekly or per-date), time-off blocks (>3d needs admin visibility, POLICY §5), lessons w/ hover cards, drag-reschedule, ghost cancelled, needs-attention inbox, meeting-room autofill, copy-week. |
| `/teacher/guide` | Onboarding handbook (setup, running a lesson, homework, library). |
| `/teacher/profile` | Shared `AccountCard` + assigned students / open hours / upcoming stat row, permanent meeting room (warns while unset), **introduction + IELTS toggle** (what students read before lesson one), provisional payable this month (POLICY §4), roster shortcut and links to calendar + sessions. Reached from the avatar menu. |
| `/onboarding/teacher` | Four-step wizard every invited teacher lands on: name / app language / timezone / clock / phone → meeting room + **POLICY §8 recording consent** → introduction + IELTS → weekly working hours (written as ordinary vacancies). Translated en/ru/ar. |

### Admin portal
| Page | What it does |
|---|---|
| `/admin` | Metrics + real P&L from ledger (`reports.monthlyStats`) + a compact needs-attention card (counts only, links out). |
| `/admin/staff/[id]` | An admin's own page: contact, live local clock, and the **permission list actually in force** (role defaults vs a custom override). Only the platform owner can edit another admin's access; the owner's own access is not editable from inside a tenant. |
| `/admin/attention` | POLICY §7 triage in full: unpaid lessons · schedules that will skip · credits expiring · gone quiet · never booked. Each row links where the admin acts and can be dismissed for 30 days (restorable at the bottom). |
| `/admin/people` | Students/Instructors tabs: **whole row opens the profile** (student → `/admin/students/[id]`, instructor → `/admin/teachers/[id]`; inline controls stop the bubble), pause/resume, teacher assignment, **Availability** (the real calendar board, not the old grid), invites. Instructor rows show timezone + whether a meeting room is set. |
| `/admin/sessions` (+`/deleted`) | Org-wide sessions (with teacher + student columns) + soft-delete restore. |
| `/admin/library` (+`[id]`) | Add markdown materials with **cover image, source URL and reading time**, publish toggle, ✨ Prepare (batch vocabulary pre-resolve, opt-in), reading preview. Rows show the cover thumbnail and the metadata. ⚠️ no PDF/audio file upload UI. |
| `/admin/calendar` | All-teachers overview + assign student into open slot (atomic 1-lesson deduction, buffer override with confirm). Defaults to the academy timezone; pending-reschedule count links to the queue. |
| `/admin/billing` | **Overview** (month P&L from the ledger) · **Payroll** (per-teacher owed → Mark paid) · **Expenses** (manual costs + recurring reminders) · Balances · Packs (region-grouped catalog, editor, seed, pack-aware grant) · **How students pay** (Kaspi number/name/QR shown to students) · **Money ledger** · Lesson ledger (`pointTransactions`). |
| `/admin/settings` | Branding (name, colour, **real logo upload** → Convex storage → sidebar mark) + feature toggles (Achievements/Library gate the student sidebar; the other three say "not enforced yet") · teacher invite link (copy/rotate) · **AI prompt manager with a working editor** (model / temperature / max tokens / system + user prompt, per-org upsert, reset to the built-in) · **achievements create/edit/delete** · scheduling knobs that are actually read (reschedule window, lesson length, max reschedules/month) plus a read-only panel of the POLICY-fixed rules. |
| `/admin/scheduling/requests` | Pending reschedule queue — reached from the dashboard card and the calendar subtitle. |
| `/admin/students/[id]` | Same student page the teacher sees (shared `StudentDetail`) — admin had no way into a student before. |
| `/admin/teachers/[id]` | Teacher operating view, opened from the Instructors name: contact incl. **WhatsApp deep link + their live local clock**, timezone/Meet room, assigned students (balance + next lesson), weekly open hours, recurring students, provisional payable (POLICY §4 rule), reliability counts, time off w/ acknowledge, VacancyEditor, recent sessions. Calendar button deep-links `/admin/calendar?teacher=`. |
| `/admin/profile` | Shared `AccountCard` + academy counts (teachers / students / lessons delivered), quick links, and a real access panel (role, academy, academy tz, member since). Reached from the avatar menu. |

### Backend systems (`convex/`)
| System | Files | Behavior |
|---|---|---|
| Tenancy & auth | `lib/tenant.ts`, `lib/permissions.ts`, `users.ts` | Org-scoped everything; role + per-user permission overrides; `getLearnerLocale`, `setStudentL1`. |
| Calendar & scheduling | `calendar.ts`, `schedule.ts`, `scheduleCron.ts`, `lib/policy.ts`, `lib/time.ts` | Availability = weekly `teacherVacancies` ± `slotExceptions`; lessons = `scheduleEvents` (minute-positioned, statuses + audit fields); `recurringBookings` materialized ~7d ahead by cron (skips on zero balance → notify); open ranges computed, never stored; all policy checks server-side; crons: no-show ladder, reminders (24h/1h/5min), auto-resume pauses. Every rule reads POLICY via `lib/policy.ts`. |
| Lesson balance | `points.ts`, `billing.ts` | Lesson packs → `pointGrants` (60-day expiry from first use, POLICY §2), FIFO spend at booking, refunds per policy, full ledger. |
| Live lesson & AI content | `lessons.ts`, `lessonContent.ts`, `inLessonQuiz.ts`, `lessonAudio.ts`, `ai.ts`, `soniox.ts`, `promptConfigs` | Transcript → summary/vocab/flashcards/quiz via OpenRouter; publish pushes vocab as SRS cards; audio backup to storage. |
| Reading & word bank | `library.ts` | Free Dictionary → base-form fallback (`services`→`service`) → MyMemory translation (thesaurus dumps trimmed) → manual; org-wide cache `libraryWordLookups` w/ per-language `translations`, `isValid` gibberish flag; ✨ `aiWordGloss` = meaning in THIS sentence, banked; `_backfillCardTranslation` fills cards saved without a translation; `enrichMaterialVocabulary` batch pre-pass (opt-in). |
| SRS / word list | `srs.ts`, `lib/sm2.ts` | One default deck per student; add is idempotent per word; SM-2 with reviewer-local dates; 60/session + `NEW_CARDS_PER_DAY=15` caps; `listMyWords` (state derived: new/learning/learned), `getWordSet` (paints reading), edit/remove. |
| Homework | `homework.ts`, `homeworkAi.ts`, `nodes.ts` | Draft → assign → submit → review; auto-grading (fill-blank, choice) + teacher overrides; answer keys stripped server-side pre-review; student writes merge answers only. **`dueAt` defaults to the student's next lesson** (POLICY §10 defines the deadline that way) — stored as a real instant, teacher can override or clear via `setDueDate`. |
| Notifications | `notifications.ts`, `src/lib/notificationText.ts` | Kind+payload rows → human sentences; bell in all portals. |
| Gamification | `achievements.ts`, `streaks.ts`, `study.ts` | Study sessions + quiz attempts recorded; both bump the streak (`bumpStreak`, student-local dates) and run `evaluateAchievements` — recomputes all five counters (completed lessons, reviews, perfect quizzes, longest streak, learned words) and unlocks + notifies anything newly crossed. Idempotent; `syncMine` catches up on page open. |
| Onboarding & lifecycle | `onboarding.ts`, `retention.ts`, `maintenance.ts` | Student form + trial grant; admin attention signals; batched data-wipe helper (used for 2026-07-23 pre-launch reset). |
| ICS | `http.ts` (`/ics?token=`) | Per-user token feed, correct UTC conversion, Meet link as LOCATION. |
| Dev/CI helpers | `seed.ts`, `maintenance.ts` (`_wipeOldData` / `_wipeLibrary` / `_deleteSeedUsers` / `_resetStudent`), `schedule:seedTestEvent`, `homework:_seedCli` | Internal-only; seed a tenant, reset dev data, author test rows. Watch for seed drift vs schema. |

### Data model (one line per table — full shapes in `convex/schema.ts`)
`tenantSettings` (brand/policies/flags/trial/currencies) · `users` (role, teacherId, tz, timeFormat, pause fields, meetLink, icsToken) · `lessons` + `lessonVocabulary/Flashcards/QuizQuestions` + `inLessonQuizDrafts` · `libraryMaterials` · `libraryWordLookups` (shared word bank: translations per language, baseForm, isValid) · `srsDecks` / `srsCards` (translation, translationLocale, firstReviewedAt, SM-2 fields) / `reviewLogs` · `scheduleEvents` (audit: cancelledBy/At/Charged, rescheduledBy, recurringWeekKey) · `teacherVacancies` / `slotExceptions` / `recurringBookings` · `pointPackages` / `pointGrants` / `pointTransactions` · `homework` · `notifications` · `studentOnboarding` (incl. l1) / `studentProfiles` / `studentPauses` · `achievements` / `studentAchievements` / `streaks` / `studySessions` / `quizAttempts` · `billingRecords` / `expenses` / `exchangeRates` · `certificateTemplates` / `issuedCertificates` (schema only, no UI) · `teacherInvites` / `permissionRequests` / `promptConfigs` / `rescheduleRequests` / `studentRescheduleQuota` / `makeupCredits` / `scheduleEnrollments` (groups — schema only).

---

## 4. Deployment & dev

| Piece | Value |
|---|---|
| Vercel | project `next-js-omni-class`, team `fafo-s-projects`; `git push origin master` auto-builds prod. |
| Convex prod | `valuable-loris-929` — **manual `npx convex deploy` required for every `convex/` change** (no `CONVEX_DEPLOY_KEY` in Vercel). |
| Convex dev | `quixotic-quail-572` (`npx convex dev`). |
| Clerk | dev keys (`secure-husky-22.clerk.accounts.dev`); Organizations → Membership optional. Production migration must recreate `org:teacher`, create/map the academy org, and replace both Clerk keys together. |
| Env — Vercel | `NEXT_PUBLIC_CONVEX_URL`, `NEXT_PUBLIC_CONVEX_SITE_URL`, `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, `CLERK_SECRET_KEY`. |
| Env — Convex prod | `CLERK_JWT_ISSUER_DOMAIN`, `OPENROUTER_API_KEY`, `SONIOX_API_KEY`. |
| AI browser login | `node scripts/dev-login.mjs [teacher|admin|student]` → prints one-shot sign-in URL (5-min ticket). Dev users: teacher mhd.mustafa.allahham@, admin warp.smp@, student mustafa.allham777@. |

---

## 5. Known issues (current queue — add here as FaFo dumps them)

### Open
- **Launch auth configuration:** create the Clerk production instance and academy organization, migrate the existing organization/user IDs, recreate `org:teacher`, keep organization membership optional, replace both Clerk keys in Vercel, then verify a fresh student signup and teacher invite. Add the final custom domain in Vercel and Clerk before announcing the URL. Runbook: `docs/PRODUCTION_AUTH_AND_DOMAIN.md`.
- Onboarding answers are now *visible* on the student page but not yet *acted on*: wire `interests` into library recommendations and `preferredDays`/`preferredTimeOfDay` into the teacher's slot-painting hints.
- POLICY §10 teacher-side obligations still unbuilt: publish notes within 24h of lesson end, and unreviewed submissions surfacing in the teacher's needs-attention view.
- Lesson vocabulary rows created before 2026-07-29 have no `definition` (the field is new) — regenerate vocab on those lessons to fill it in.

### Admin portal (first pass done 2026-07-30 — see change log)
- Still open: Sessions "View" routes into teacher paths. Library: no PDF/audio upload. Gamification / Live Quiz Generation / Payments toggles save but nothing reads them yet. No "test this prompt" run-against-sample button in the AI manager (editing works).

### Mobile navigation (reviewed 2026-08-07 — decision: keep both)
- The hamburger drawer and the bottom nav are **not** redundant. The drawer carries all 8 student destinations; the bottom nav carries the 5 daily ones, thumb-reachable, one tap instead of two. Removing it would push the most-used routes into the worst reach zone on a phone (top-left) and cost a tap each time.
- Four items overlap (Home · Lessons · Study · Calendar); Library, My Words, Achievements and Lessons & packs are drawer-only, and Profile is bottom-nav-only. That split is correct — primary vs secondary, not duplication.
- If vertical space on the calendar becomes the complaint, the fix is **auto-hide on scroll-down / reveal on scroll-up**, not deletion.

### Payments (Kaspi path, decided 2026-08-07 — see POLICY §3)
- Kaspi merchant API is **blocked on the ИП existing**. Until then payment is manual: student reads the Kaspi details off the billing page, pays, academy grants the pack. Automation reuses `paymentEvents` → `fulfillOrder` unchanged.
- **Unanswered:** does Kaspi internet-acquiring accept Saudi-issued cards? Ask Kaspi Bank in writing once the ИП is live. Gulf students are deferred to the Stripe phase regardless.
- `convex/payments.ts` (Lemon Squeezy) is complete but **not the launch rail** — kept because `fulfillOrder`, `claimEvent` and the event ledger are provider-agnostic and Kaspi will feed them.
- Whoever operates payments should get **scoped permissions** (`billing.view`, `billing.edit`, `users.create`, `users.edit`), not blanket admin — `users.setUserPermissions` already supports it.

### Cross-cutting
- `users.listAllUsers` used as client-side name lookup in several teacher pages — ships all org emails to any client. Resolve names server-side (calendar already does).
- POLICY [PROPOSED] unbuilt: expiry warnings 14/3d (§2), homework 24h obligation (§10). Recording consent (§8) is now taken from **both** sides at onboarding.
- i18n: the **whole student portal is translated (en/ru/ar)**, plus **teacher onboarding**. The rest of the **teacher and admin portals are still English-only** — internal, and their copy is still moving. Lesson content, AI summaries and dictionary definitions stay English by design; only flashcard answers use the student's L1.
- Groups, certificates, payment gateways (Lemon Squeezy → Stripe), teacher payroll: schema-ready or planned, no UI — post-launch.
- Tech debt: transcript passed via global `window` bridge on live/review pages; `any`-typed map callbacks in older pages.

---

## 6. Product decisions already made (don't relitigate)

Reading = collecting into one word list (no per-word statuses) · flashcards drawn only from the list, translation-first, 15 new/day · lessons not points · v1 = 1-on-1 online lessons only (activity types return post-v1) · Google Meet links manual (OAuth removed) · one unified calendar, availability painted not scheduled · recurring weekly slot is the core booking object · reschedule ≠ cancel; consequences always shown before confirm · English definitions v1, learner-L1 translations automatic · no auto student-lifecycle statuses (admin attention list instead, POLICY §7) · certificates/groups deferred until FaFo asks · Reports tab removed (folded into Students).

---

## 7. Change Log

> Older history (2026-05 → 2026-07-29, the phased build era) lives in git: `git log --follow MASTER_PLAN.md`, plan version `804bdfc` and earlier.

| Date | Change |
|---|---|
| 2026-09-03 | **[Codex]** **Public signups now join the academy automatically.** A new authenticated auto-join route resolves the deployment's single signup tenant, creates an idempotent Clerk `org:member` membership, and the client explicitly activates it so Clerk and Convex immediately receive the `org_id` claim; fresh students land on student onboarding instead of an empty organization selector. Teacher invites retain priority and now use Clerk's explicit `org:teacher` role, update existing memberships safely, refresh the active organization before the Convex role write, and keep the invite cookie until finalization succeeds—preventing invited teachers from silently becoming students. Tenant selection fails closed if a deployment ever contains multiple academies, existing active-org users cannot be attached elsewhere, and server errors no longer leak provider responses. Clerk's development instance was changed from required to optional membership so app-side auto-join can run. Verified on the deployed Vercel site with disposable student (`/onboarding/student`, `org:member`) and invited-teacher (`/onboarding/teacher`, `org:teacher`) accounts, plus targeted ESLint, TypeScript, and the production build; their Clerk identities and Convex rows were deleted afterward. Full-repo lint remains blocked by 279 pre-existing errors outside this change. |
| 2026-08-07 | **[Claude]** **Mobile stopped wasting a third of the screen on padding.** On a 375px phone the calendar's day column was getting **227px** — 56px eaten by `main`, 32px by the card, 60px by the time gutter. Three fixes, all tokens rather than one-off values: `main` had a **hardcoded `padding: 28px` inline**, which ignored `--pad-page` entirely (that variable already dropped to 14px on mobile and nothing was reading it), so page padding is now its own token `--pad-main` — 28px desktop, 14px mobile, desktop byte-identical. The calendar time gutter became `--cal-gutter`, 60px → **44px** on mobile. And card padding, which is set inline across the app anywhere from 14px to 40px, is normalised to **16px on mobile** with `!important` — inline styles beat a class, and on a phone that variation reads as inconsistency rather than hierarchy. Day column goes **227px → 271px** (61% → 72% of screen); cards that were at 24–40px gain another 16–48px. Verified in the compiled CSS and bundle — the Browser pane was wedged this session, so **no visual mobile screenshot was taken**. |
| 2026-08-07 | **[Claude]** **Mobile shell was scrolling the document instead of the page.** The root was `minHeight: 100vh`, which caused two bugs at once. `100vh` on a phone means the viewport with the browser chrome *expanded*, so once the URL bar collapsed the document ran taller than the screen and left a **white strip under the fixed bottom nav**. And because the root carried only a *minimum* height, `main`'s `overflow-y-auto` never became a scroll container — the whole document scrolled, so the calendar grid's sticky day header and the sticky topbar both pinned to the viewport top and **the calendar drew over the topbar** (it held `z-20`, the topbar `z-10`). Root is now `height: 100dvh`, which makes `main` the real scroll container, and the topbar moved to `z-30` — above both the calendar header and the bottom nav, since nothing in a page should scroll over the app frame. Verified on a 375×812 viewport: document no longer scrolls, `main` does, and root height equals `innerHeight`. **The mobile bottom nav stays** — see §5 for why the apparent redundancy with the hamburger drawer isn't one. |
| 2026-08-07 | **[Claude]** **Payments turn toward Kaspi; the billing page answers "where do I send money".** Lemon Squeezy is out — its terms cover digital products and courses, not scheduled 1-on-1 human services — so POLICY §3 was rewritten: **Kaspi for Central Asia** (an ИП + Kaspi Pay, manual first, merchant API later), **Stripe via a US LLC for Gulf and rest-of-world, deferred**. The built Lemon Squeezy integration stays in the tree because the shape is right: `fulfillOrder` and `claimEvent` never knew which gateway they served, so Kaspi feeds the same `paymentEvents` ledger and the same `grantPointsInternal` path when its API arrives. `paymentEvents.provider` widened from a literal to `lemonsqueezy | kaspi | manual`. **What ships now is the manual half POLICY §3 v1 always described but nothing implemented:** `tenantSettings.manualPayment` holds the academy's Kaspi number, the recipient name and a QR (Convex storage, replaces delete the old file), edited from a new **How students pay** tab in admin Billing and rendered as a **How to pay** card on the student's packs page — translated en/ru/ar. Gated on the academy's own switch rather than on whether a card gateway happens to be configured, because during a provider move both are true and the toggle is the intent. Two details that matter: the card names the **recipient** and tells the student to check it, since a Kaspi transfer to the wrong person can't be undone; and the phone number is `dir="ltr"` isolated after Arabic rendered it **`33 22 111 700 7+`** — the clipboard was always right, the eyes were not. Verified as admin (set details → saved) and as student in all three languages. |
| 2026-08-07 | **[Claude]** **Teacher onboarding, audited and finished.** The wizard existed but **the one audience it was built for never saw it**: `acceptTeacherInvite` set `onboardingComplete: true` at the moment the invite was accepted, and post-signup hard-navigated to `/teacher/calendar` — two independent blocks, either fatal on its own. Accepting an invite now only makes you a teacher, and post-signup routes to the wizard; a teacher who has already finished it is bounced to `/teacher` instead of re-running it. **The mutation stopped trusting the client**: meeting link, `HH:mm` shape, start-before-end and weekday range are all validated server-side, and the shape check runs before any write so a rejected range can't leave half the days inserted. The availability **clash check was wrong twice** — non-indexed `.filter()`, and it matched *deactivated* rows, so a teacher who had switched Monday off could never reopen it and the wizard reported success having written nothing; it now uses `by_organization_and_teacherId_and_dayOfWeek`, counts only active rows, and returns `slotsAlreadyOpen` so the toast says "those days were already open" instead of a silent success. **Content**: grew from 3 steps to 4 — display name, app language, clock format, **POLICY §8 recording consent** (the student always signed one; the teacher, who is on every recording, never did — stored as `recordingConsentAt`), a short **introduction** students read before lesson one, and **IELTS certified**; the last two are editable afterwards on the teacher profile. Prefill now reads real availability rather than defaulting to Mon–Fri and then skipping it as a clash. **Timezone is a `<select>`** (`TimezoneSelect`, all ~400 IANA zones with UTC offsets, the academy's common ones on top) replacing a free-text box duplicated in two files — and `tzOffsetMin` was returning fractional offsets (`UTC+04:59.96`) because it differenced minute-precision parts against a millisecond instant. **Locale finally persists**: the switcher only ever wrote `localStorage`, so Arabic on the laptop meant English on the phone and `users.locale` was never filled by anything; `LocaleSync` hydrates from the account on load and writes back on change. Wizard chrome translated (en/ru/ar) with RTL-aware chevrons, and its touched-state effect replaced by derived state; the stale `onboarding.*` message block — 33 keys from a student form that no longer exists — was replaced wholesale. Dev helper `maintenance:_resetTeacher` added. Verified end to end in the browser as a real invited teacher, in Arabic and English, including the re-run path. |
| 2026-08-02 | **[Claude]** **POLICY verdicts speak the student's language.** Cancellation and reschedule consequences were generated server-side as finished English sentences ("Free cancellation (1 left this month)"), so they stayed English no matter the locale — the one place a mistranslation costs money. `cancelVerdict`/`rescheduleVerdict` now return **`reasonKey` + `reasonValues` alongside the English `reason`**: the sentence as a message key, its numbers as arguments. `usePolicyText` (`src/lib/policyText.tsx`) renders the key, translates the embedded lesson **status** enum before it lands inside the sentence, and **falls back to the English sentence** when a key is unmapped — a missing translation degrades to a real sentence, never a key path or a blank line. Wired into the student calendar's move/cancel hints and the cancel confirmation; the thrown `ConvexError` and the staff portals keep the English string deliberately. 21 policy keys × en/ru/ar, checked programmatically. ⚠️ Not exercised live: seeded lessons don't appear on the student calendar in dev, so the dialog itself is unverified in the browser. |
| 2026-08-02 | **[Claude]** **Study and calendar finished in all three languages.** The earlier passes translated headings but left the working parts of both pages English. Study: the hub (homework card and its per-item states, flashcards card, reading list), and the whole session — progress line, card counter, streak chip, Again/Hard/Good/Easy, "reveal answer", and the completion screen's three stats. Calendar: the booking dialog (repeat-weekly line, no-balance warning, the "no bookable start" explanation with its policy numbers), the lesson dialog (move / cancel / stop weekly, the cancel confirmation), Meet link and the weekly-schedule note, plus the grid's own "My lesson" label. **Material kinds** (article / story / dialogue / transcript) were raw enum values everywhere — now translated on the study reading list and on every library card, along with the card's read-time and source labels. Catalogue is 848 keys with **zero missing in ru or ar**. |
| 2026-08-02 | **[Claude]** **Russian joins the switcher.** `ru.json` held the pre-rebuild catalogue and was missing all 238 keys the current student portal uses, so Russian would have rendered as raw key paths — that's why it was held back. Translated in full (dashboard, study, My Words, library, lessons, homework, calendar, achievements, packs, profile, clock, nav and role labels) and verified against en for zero missing keys. Locale list is now **en · ru · ar**, LTR/LTR/RTL. |
| 2026-08-02 | **[Claude]** **Student portal finished in Arabic.** Second pass over the surfaces the first one left in English: calendar (booking dialog, legend, move flow, every toast), lessons list + filters, homework sections and their subtitles, the full profile including the edit dialog, and the shared `PersonTime`. Dates now follow the active locale — the clock read "Mon" under Arabic copy; it reads **الاثنين** now — and language names are translated rather than printed as `Arabic`. RTL leftovers fixed: sidebar labels and the study grid used `textAlign: left` and the collapsed badge used `right`, so they stayed pinned to the wrong edge under `dir="rtl"`; all now logical (`start` / `insetInlineEnd`). View switcher (Day/Week/Month) and role labels translate too. |
| 2026-08-02 | **[Claude]** **Arabic, for real.** The language switch wasn't broken so much as pointless: `ar` wasn't in the locale list at all (`["en","ru"]`), and only 4 components in the whole app called `useTranslations` — everything else was hardcoded English, so switching changed nothing visible. Now **English + Arabic**, Arabic flipping `<html dir="rtl">` (verified: the whole shell mirrors, sidebar to the right). **Nav is translated for every portal** via each item's key with the English config label as fallback, so a missing key degrades to English instead of printing `nav.people`. **The student portal is translated end to end** — dashboard, My Words (including table headers and both empty states), Library, Study, Achievements, Lessons & packs — with the catalogue extended to match the copy that actually ships today; the old 556-key catalogue described a UI from before the rebuild. Russian stays out of the switcher rather than shipping half-translated. Staff portals remain English on purpose: they're internal, and their copy is still moving. |
| 2026-08-02 | **[Claude]** **The academy's money, as a system.** Before this the business had no books: revenue was *recomputed live from today's pack prices* (so editing a price rewrote last month), the `expenses` table existed with nothing writing to it, and teacher pay was an estimate — average pack price × 30% — that nothing recorded or settled. Now: **`financeEntries` is one ledger for everything in and out**, written when money moves and never recomputed. **Pack sales book themselves** inside `grantPoints` at the price the pack carried that day (idempotent per grant; trials, make-ups and manual grants carry no price and aren't booked as income). **Transcription accrues monthly** from lessons × the Soniox rate, labelled an estimate because that's what a meter reading is. **Everything else is entered by hand, with `financeReminders`** — salary, ads, subscriptions, rent — where a daily cron notifies admins and the attention page grows a **"Money to record"** section; entering the cost from the reminder marks its period done, and "nothing this time" is a first-class answer. **Payroll pays per lesson done** (POLICY §4 payable set: completed + charged student no-shows) at a **flat per-teacher rate** (`payoutPerLesson`, academy default behind it). A `payrollRuns` row **stores the exact lesson ids it paid**, which is what makes "unpaid" self-correcting: no reset button, partial months work, retroactively-completed lessons appear in the next run, and the same lesson can never be paid twice. Approving writes the run and its salary ledger row in one transaction, notifies the teacher, and can be undone. Teacher and admin pages now show **owed / last paid** instead of a guess, and People's Instructors tab gained an **Owed** column. Verified end-to-end: seeded lessons → set 12 USD/lesson → Mark paid → unpaid 1→0, salary in P&L; granted an 8-lesson KZT pack → $60 income booked automatically; recorded a 150 ads cost via its reminder → reminder flipped to done. |
| 2026-08-02 | **[Claude]** **Everyone's local time, next to their name.** The previous pass put a clock in the topbar showing *your own* time, which your screen already tells you — dropped. Time now appears where it isn't obvious: one shared `PersonTime` renders `05:22 pm · Sun · their time` (hover = the zone) on the student page, the teacher page, an admin's staff page, the teacher's student roster, and as a **Local time column on all three People tabs**, so a glance down the table says who's awake without any mental arithmetic. Your own profile shows the same line as **your time**, because a wrong timezone silently shifts every lesson time you read — and when it's unset the row says **"No timezone — set it"** in amber and links to the page that fixes it, instead of sitting blank. People's header carries an **Academy 05:21 pm · Asia/Almaty** chip: lessons are stored in academy wall-clock, so that's the reference the columns are read against. |
| 2026-08-02 | **[Claude]** **Management team, platform owner, and the time it is now.** **People gained an Admins tab** — the people who run the academy were the one group with no list: name, email, phone, their **live local clock**, whether they run on admin defaults or a custom access list, and a row that opens **`/admin/staff/[id]`**, where permissions are shown as the literal keys in force and can be edited. **`warp.smp@gmail.com` is the platform owner** (`convex/lib/superadmin.ts` — membership is code, not a database row that someone could edit): the account is badged everywhere, and `updateUser`, `deleteUser` and the new `setUserPermissions` all refuse to demote it, delete it or touch its access. Editing *another* admin's access is owner-only, so tenant admins can't strip each other. This marks and protects the account; it does not by itself read across tenants — every query is still org-scoped by the Clerk session. **A live clock now sits in the topbar of all three portals** (viewer's timezone and clock format, tabular figures so it doesn't jitter, date underneath), and the **teacher page shows the teacher's WhatsApp as a `wa.me` link next to their own ticking clock** — nobody should do timezone maths before messaging a teacher. |
| 2026-08-01 | **[Claude]** **People rows open profiles; admin availability editing uses the real calendar.** Clicking a person did nothing unless you hit the name text exactly — the **whole row navigates** now (student and instructor), with the teacher dropdown, Edit, Pause, Availability and the meeting-room link stopping the click so they still work. And the "Vacancies" editor behind People and the teacher page was the **H.7 half-hour checkbox table** — a second, older picture of availability that could disagree with the calendar everyone else reads. Replaced by **`AvailabilityBoard`**: the same `WeeklyCalendar` grid, open/block brushes, weekly-or-single-date scope, undo snackbar, lessons and bands visible while you paint. `calendar.setSlotsBulk` gained an optional `teacherId` (admin-only; a teacher still can't touch anyone else's) since it previously hardcoded the caller as the target. The board is pinned to **academy time** rather than the admin's own zone — painting 18:00 has to mean the teacher's 18:00 — and says so under the toolbar. Legacy `VacancyEditor` deleted. Verified as admin: painted a slot from the teacher page ("Opened 1 slot every week"), undid it, and opened the same board from People without the row navigating away. |
| 2026-08-01 | **[Claude]** **FaFo dump: attention triage, profiles, library metadata, student packs.** (1) **Needs attention left the dashboard.** It also lied: the "gone quiet" rule counted a student with credit and no completed lesson as dormant, so **every fresh signup with an unused trial appeared the day they joined**. Split into two signals — *gone quiet* (had lessons, credit left, nothing for 14 days) and *never booked* (holds credit, nothing on the calendar, and only after a 7-day grace; a student who has already booked never appears at all). The full list moved to **`/admin/attention`** with a row-level **dismiss for 30 days** (`attentionDismissals`, restorable at the bottom of the page); the dashboard keeps a count card whose chips link into it. (2) **Profiles.** Phone/WhatsApp is now editable by every role, and a student's **native language is read-only for them** — it decides what every flashcard they own is translated into, so staff set it (`l1` removed from `updateMyProfile` entirely; `setStudentL1` is the only path). Student profile also stopped saying "Points balance / Active points" — it's **lessons**, per the standing rule — and its buy button now goes to the new packs page instead of a mailto. (3) **The tenant chip beside the language switcher is gone** — one admin runs one academy, and the sidebar already carries the brand. (4) **Library metadata no longer dead-ends both ways:** the admin form couldn't set `sourceUrl` or `estimatedReadMinutes` although the student card rendered them, and there was no cover image at all. Admin can now set cover (Convex storage, 3MB), source and reading time; teacher and student grids share one **`MaterialCard`** that shows cover, kind, level, description, reading time, source and topic tags; the reader credits the source. Admin rows show the thumbnail and the same metadata. (5) **Deleting a library material had never worked** — `tenantTable.softDelete` writes `deletedBy` and the `libraryMaterials` schema had no such field, so every delete failed validation. Fixed. (6) **`/student/billing`** — the real pack catalogue at real prices with a Request button that notifies every admin (`points.requestLessons`, new `lessons_requested` notification); no fake checkout while payments are unintegrated. (7) Library sidebar icon aligned across portals (admin was `layers`, everyone else `book`). Verified as admin and student: dismiss/restore round-trip, request → admin bell, cover upload → card → reader, delete. |
| 2026-08-01 | **[Claude]** **Admin Settings — the dead half now works, and the lying half is gone.** **Logo upload was a dashed rectangle with no file input**; it uploads to Convex storage now (`generateLogoUploadUrl` / `setLogo` / `clearLogo`, 1MB cap, PNG/SVG/JPEG/WebP, replaced files deleted rather than orphaned) and the **sidebar mark reads it** — it was a hardcoded `/logo-mark.svg`, against the no-hardcoded-branding rule. Only a real upload counts: the seeded `logoUrl` points at a repo asset, so `logoStorageId` is what distinguishes "the tenant chose this" from "the default", and the bundled mark stays until someone uploads (verified end-to-end: uploaded an SVG, watched the sidebar change, removed it, watched it revert). **The AI prompt manager was read-only cards** — `promptConfigs` had queries and no writer at all. New `upsert` (admin-only, `ai.configure`) edits model / temperature / max tokens / system prompt / user template, and because the list mixes stored rows with code fallbacks for configs an org was never seeded with, editing a fallback inserts the row; `resetToDefault` drops the override again. Each card says whether it's **Edited** or **Default**. **Achievements Edit was a button with no handler** and `create` had no UI at all: both now open a dialog (icon, name, description, counter with human labels instead of `lessons_completed`, threshold, reward) backed by a new `achievements.update`. Fixed a bug the verification pass caught in my own code: the dialogs weren't keyed, so opening a second prompt kept the first one's text under the new title — the title said Lesson Summary while the body was the quiz prompt. **Scheduling Policies was half fiction:** "Cancel Window" and "Student no-show consumes a lesson credit" wrote to `tenantSettings` and **no code has ever read either** — cancellation notice is 6h/12h and no-show handling is fixed in `lib/policy.ts` from POLICY.md. Both controls are gone; the three knobs that are genuinely read (reschedule window, lesson length, max reschedules/month) stay, and underneath them a read-only panel now lists what the server actually enforces (new `policyConstants.get`), so the page can't promise a rule it doesn't apply. §5 also corrected: the teacher invite link UI and the `/sign-up?invite=` wrapper were listed as missing but have both been built for a while. |
| 2026-08-01 | **[Codex + Claude]** **Staff pages: teacher detail for admin, working profiles for both.** Drafted by Codex (ran out of usage before verifying or shipping), finished, fixed and shipped by Claude. **New `/admin/teachers/[id]`** — the last unreachable person in the admin portal: Instructors names were plain text, so a teacher had no page anywhere. One protected query (`users.getTeacherDetailForAdmin`, admin-only, tenant-scoped) returns roster with balances and next lesson, weekly open hours + active days, recurring students, meeting-room state, reliability counts (student/teacher no-shows, teacher cancellations), homework awaiting review, upcoming time off, and the last 8 sessions; the page adds the VacancyEditor and a Calendar button that deep-links `/admin/calendar?teacher=`. **`/teacher/profile` and `/admin/profile`** stop being bare account cards — the teacher's shows assigned students, open hours, upcoming lessons, meeting room (with a warning while unset) and provisional payable; the admin's shows academy counts and a real access panel (role, academy, academy timezone, member since) instead of two paragraphs of prose. **Payout is labelled provisional and calculated by POLICY §4** — completed lessons and charged student no-shows only, at `payoutRateOverride ?? 30%`; `reports.teacherEarnings` now takes an optional `teacherId` so admin sees the same number the teacher does, with a teacher-can-only-see-their-own guard. **Four fixes on top of the draft:** (1) `teacherEarnings` **threw** "Teacher not found" whenever the target wasn't a teacher, so an admin opening `/teacher/profile` got a white "Something went wrong" page — it returns `null` now, since a throwing query takes the whole page with it (verified: admin loads the page, teacher loads real numbers). (2) The calendar deep-link re-applied `?teacher=` on every render, which would have pinned the dropdown and undone each manual switch — applied once via a ref. (3) `vacancies.listForTeacher` gained the missing ownership check (a teacher could read any teacher's availability by passing an id); only VacancyEditor calls it, teacher and admin both verified. (4) People's **"Lessons"** column counted lesson *records* while the new teacher page shows lessons *left* — the same student read 0 and 4 on two screens; the column is **Sessions** now. |
| 2026-07-30 | **[Claude]** **Admin portal pass — fake numbers out, dead controls out, unreachable tools linked.** Walked every tab as admin. **Lies removed:** the dashboard's "AI Prompts Used: 1948" was `promptConfigs.length * 487` — a literal invented number — now a **Pending Reschedules** card that links to the queue; Settings' "Total cost per lesson $0.000000" summed a `costPerLesson` field **no code ever writes**, so it now shows the real Soniox transcription cost ($0.402/lesson at 60min) and says LLM cost isn't metered yet. **A wrong warning:** the student page claimed "Native language not set — flashcards can't be translated" for a student whose `users.locale` is `ar`, because `profile.l1` read only `studentOnboarding.l1` while the flashcard path (`resolveLearnerLocale`) also falls back to `users.locale`; both use the same chain now, so the page can't contradict what the cards actually do. **Every Select showed raw values instead of labels** — the teacher dropdown read `user_3DJgy8SGelsEJvXD0VzQm1d88PI`, the Edit dialog read `student`/`trial` — Base UI needs `items` on `Select.Root` for `Select.Value` to render a label; passed it everywhere. **Dead controls deleted:** the People **Permissions** tab was a hardcoded matrix for Manager/Sales/Support roles that don't exist in this system (admin/teacher/student only). **Dead controls wired instead:** the five Settings feature toggles saved to `tenantSettings`, were exposed via `brand.feature()` and read by **nobody**; sidebar items now carry an optional `feature` key that `PortalShell` filters on, so Achievements and Library really do disappear from the student portal (verified: toggled Library off, watched it leave the sidebar, toggled it back). The three with no consumer yet say so on the row rather than pretending. **Unreachable tools reached:** `/admin/scheduling/requests` (approve/reject reschedules) had **zero links anywhere in the app** — requests would have piled up unseen; now linked from the dashboard card and from the calendar subtitle. Billing's **Records** tab was a "payment integration deferred" placeholder while the `pointTransactions` ledger sat unread — new `points.listOrgTransactions` backs a real table (date, student, type, ±lessons, balance after, who did it, why), verified against a CLI grant. **Sessions** gained Teacher and Student columns — an admin could not tell whose lesson a row was. **Copy/UI:** the admin's vacancy editor said "My weekly availability" while editing someone else's (now "<name>'s weekly availability"); People's "Last Activity" column actually showed the signup date (renamed **Joined**); Library said "Upload" with no uploader (now "Add"); its two native `<select>`s became the app's Select; student names no longer wrap mid-word. Admin calendar now defaults to the **academy** timezone rather than the browser's guess. |
| 2026-07-30 | **[Claude]** **Onboarding answers surfaced, then both deployments wiped clean.** The signup answers were being stored and read by nobody. `getStudentDetailForTeacher` now returns them all and the student page shows a **"From their signup"** panel: days they can study, times of day, topics, their own words, referral, signup date, and **recording consent with its date** (POLICY §8 proof, green when given / amber when not). Availability is the one a teacher acts on — slots get opened against it. The page body moved into a shared `StudentDetail` component and now also mounts at **`/admin/students/[id]`**, linked from the admin People rows — admin previously had no way into a student at all. Hardened the tag renderer against missing fields so a profile can't white-screen on an older backend. **Data wipe (both `quixotic-quail-572` and prod `valuable-loris-929`):** 315 rows deleted in prod, plus library material, and the same in dev; `mustafa.allham777@gmail.com` reset to day one in both and lands on the onboarding wizard (verified end-to-end: onboarded, confirmed the teacher sees every answer, then re-wiped). **No other students existed to delete** — prod and dev each hold exactly the three real accounts (student, teacher, admin). Kept: tenant settings, 11 achievement definitions, 4 prompt configs, lesson packs. **Teacher availability was wiped in prod too** (49 rows) — repaint it before booking works. |
| 2026-07-29 | **[Claude]** **Onboarding rebuilt for both roles.** Student onboarding was one long form; **teacher onboarding didn't exist at all** — an invited teacher landed on the calendar with no timezone (so every time they read was a guess), no meeting room (so lessons had nowhere to happen) and no availability (so nobody could book them). Both are now 3-step wizards on a shared `Wizard` (progress bar, per-step validation, and room for the sentence that says *why* each answer is wanted). **Student:** contact + auto-detected timezone · CEFR as described choices rather than bare codes + native language + goal + interests · availability chips + referral + **POLICY §8 recording consent**, stored with its timestamp — the first [PROPOSED] policy item to actually ship. **Teacher:** timezone (against the academy's) · Meet link · weekly hours, which write real `teacherVacancies` (verified: 5 slots created). Schema gained `preferredDays`, `preferredTimeOfDay`, `interests`, `country`, `referralSource`, `consentAcceptedAt`. **Three bugs found by testing the flow:** (1) the welcome screen promised **5 free trial lessons and granted 0** — `getTrialPolicy` carried its own inline fallback while the granting mutation had none, so a tenant without an explicit policy row lied to every new student; both now read `DEFAULT_TRIAL_POLICY` and the mutation returns what it actually granted so the toast can't drift again. (2) Multi-select chips lost clicks — each handler closed over the same stale array, so picking three interests saved one; now functional updates. (3) An unauthenticated visit to either page crashed with "Something went wrong" because the queries fired before auth resolved; they now skip until there's a user. Also: the "what's missing" hint no longer nags before you've typed anything. |
| 2026-07-29 | **[Claude]** **Dev data reset** (dev `quixotic-quail-572` only — production untouched). Ran `_wipeOldData` (42 rows: lessons, cards, decks, homework, schedule events, streaks, achievements-earned, notifications, word-bank cache, study/review logs) and added two switches it deliberately lacks: `_wipeLibrary` (library material is academy *content*, so it needs its own explicit switch) and `_deleteSeedUsers` (dropped the "Test Student" placeholder — a seeded `seed-…` externalId has no Clerk identity behind it and can never sign in). New `_resetStudent` clears the flags that outlive a history wipe — `onboardingComplete`, status, pause fields, ICS token — so student 1 now walks the real onboarding flow, native-language question and trial grant included; the teacher pairing stays, being academy setup rather than student history. Kept: the three real people, tenant settings, 11 achievement definitions, 4 prompt configs, 6 lesson packs. **Teacher availability was wiped too** (it lives in the transactional set) — repaint it before testing booking. Fixed a rule violation spotted on the way through: the onboarding screen promised "5 free trial points" — the copy says **lessons** everywhere now. |
| 2026-07-29 | **[Claude]** **FaFo dump — speed, profiles, homework, vocabulary.** (1) **Every tab switch refetched from scratch** because plain `useQuery` drops its subscription on unmount. Added `ConvexQueryCacheProvider` and moved all 40 call-sites to the cached hook; returning to a page now renders with data already in hand (measured: the stat that used to flash empty reads its real value on the first frame). (2) **Countdowns replace clock times** — "2 hours 31 minutes until your lesson" on the student dashboard and a matching line on the teacher's, ticking every 30s (`src/lib/countdown.ts`); the exact local time stays underneath. (3) **One menu behind the avatar for every role**: Clerk's popup now carries a Profile link next to Manage account and Sign out, `/teacher/profile` and `/admin/profile` exist for the first time, and all three share an `AccountCard` (name / timezone / clock / native language) so a teacher's settings can't drift from a student's; the duplicate sidebar Profile entry is gone. (4) **Homework text no longer deletes itself while typing** — the debounced save echoed back a new object, the editor treated it as an external change and overwrote everything typed since; now the echo is ignored, incoming content never lands while the editor has focus, and the editor only re-creates on a genuinely different document. Verified: 134 characters typed across two bursts survive the save round-trip intact. (5) **Homework AI generated only blanks + a separate quiz** — one prompt now builds a mixed worksheet (gap-fill from the transcript, multiple choice, open writing), which is what the editor has supported all along. (6) **Homework ships with Publish**, like summary and vocabulary: the separate "Assign to student" button is replaced by **Approve**, `lessons.publish` sends every approved draft (with the due date), and reopening a lesson pulls its homework back to editable — unless the student already started it, whose work is theirs. (7) **Vocabulary tab drops Locale / POS / IPA** (never shown to anyone) **for an English definition**, so a word from a lesson arrives with the same shape as one collected from the library — translation to study from, definition for context. The AI vocab prompt asks for it, and published cards carry it onto the back. |
| 2026-07-29 | **[Claude]** **Homework due dates, end to end.** `dueAt` had been in the schema since Phase J with nothing ever writing it. Rather than make a teacher invent a date every time, **assigning defaults the deadline to the student's next scheduled lesson** — POLICY §10 defines the obligation as "check the student's submitted homework before the next lesson", so that lesson *is* the deadline. Computed server-side from the soonest scheduled/makeup event via `wallTimeToMs` and stored as a real instant (never a bare date string — rule §1); no upcoming lesson means no deadline, which is honest. Teacher can override with a picker at assign time or change/clear it afterwards (`homework.setDueDate`); the notification payload carries the date. New `src/lib/homeworkDue.ts` renders one consistent phrasing everywhere — "Due today at 10:00 PM" / "Due tomorrow" / "Due Friday" / "Was due Jul 27" — shown on the Study hub, the homework list and the homework page itself, and only while it's still the student's move. **Overdue is amber, never red, and never scolds:** POLICY §10 says homework completion is a retention signal for the teacher, never something the student is punished for. Verified end-to-end: seeding an assignment with no date picked up the next lesson's instant automatically; overdue and due-today pills render correctly on all three surfaces. Dev helper `homework:_seedCli` added to author+assign without the teacher UI. |
| 2026-07-29 | **[Claude]** **Achievements unlock for real; lessons page becomes a history.** (1) **Unlock engine** (`evaluateAchievements` in `convex/achievements.ts`): recomputes all five counters from their true sources — completed *schedule events* (attendance, not published notes), `reviewLogs`, perfect `quizAttempts`, **longest** streak (a week earned in March stays earned), cards with SM-2 interval ≥ 21d — and inserts any newly-crossed unlocks plus an `achievement_unlocked` notification. Deliberately recompute-everything rather than incremental counters: at ~50 students it's a few reads and it can't drift. Called from `recordSession`, `recordQuizAttempt` and lesson publish, plus `syncMine` on page open for counters that moved elsewhere. `listForStudent` now returns live progress, so the bars mean something (they were hardcoded `0%`), sorted closest-to-done first. Verified: reviewing a card unlocked First Review; seeding two completed lessons then opening the page unlocked First Steps and showed "2 / 5 lessons". (2) **My Lessons rebuilt as lesson history** (`lessons.myLessonHistory`): the page listed only published notes, so a student who'd had ten lessons with no write-ups saw nothing. Now every schedule event — Done / Upcoming / Make-up / Cancelled / "You missed it" / "Teacher missed it" — in the student's own timezone, with teacher name, filters (All/Upcoming/Completed/Missed) and search; rows that have published notes link to them, the rest are inert. (3) Dashboard "Lessons completed" already switched to completed events in the previous wave — the only honest source, since notes are optional paperwork. (4) Dev helper `schedule:seedTestEvent` gained `studentEmail` / `dayOffset` / `status` / `title` so a history can be seeded for a specific student. |
| 2026-07-29 | **[Claude]** **Student portal wave 1 — dead things now work.** (1) **Streaks were dead code**: `_updateStreak` had no caller anywhere, so every streak was permanently 0 across six displays. Rewrote `streaks.ts` around an exported `bumpStreak` called from `study.recordSession` + `recordQuizAttempt` (studying IS the bump — no client mutation to game), counting days in the STUDENT'S timezone; yesterday-math on local date strings. Session header + done screen now show the real value (verified: finished a session → "1 days"). (2) **Dashboard**: wall-time bug fixed (next-lesson found by real instants via `zonedToInstant`, shown in the student's tz + clock format); "Join in N min" only within 2h, otherwise the actual date; Meet button renders only when the event has a link; "Lessons completed" counts completed schedule events, not published notes; streak stat card (duplicate of header) replaced with **Lessons left** (red at 0). (3) **Profile**: Edit dialog (name / timezone / 12-24h / **native language** — self-service at last, saves via new `users.updateMyProfile`, L1 change sweeps old untranslated cards), real Clerk sign-out, "contact provider" is now a mailto to `tenantSettings.supportEmail`. Verified: set Russian → subtitle updates → set back to Arabic. (4) **Study honesty**: done-screen counts unique cards (an "Again" re-drill no longer inflates reviewed count or sinks accuracy — first rating is the honest one), dead "All Due" select removed, fake interval labels ("4 · 4d") dropped, and the keycaps finally work: Space/Enter flips, 1–4 rate (fixed a hooks-order crash from putting the effect after an early return). (5) Lessons page: dead All/Past tabs and raw "published" pills removed; sidebar Library icon `layers`→`book`, My Lessons `book`→`file`. |
| 2026-07-29 | **[Claude]** MASTER_PLAN rewritten as a minimal platform reference (inventory + rules + known issues) matching FaFo's workflow — no phases/steps/audit checklists. CLAUDE.md updated to current stack (Convex/Clerk, not Zustand/Supabase). Deleted `Omnica-new-UI/` prototype (1.1MB, long since ported; recoverable from git). Logged student-portal audit into §5. |
